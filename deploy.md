// 部署脚本示例（Netlify/Vercel/静态站点均可用）
// package.json scripts 已有 build
// 可选：添加 deploy 命令（如使用 vercel-cli 或 netlify-cli）

// Vercel 部署
// npm install -g vercel
// vercel --prod

// Netlify 部署
// npm install -g netlify-cli
// netlify deploy --prod --dir=dist

// GitHub Pages 部署建议用 gh-pages
// npm install -D gh-pages
// package.json scripts 增加：
// "deploy": "vite build && gh-pages -d dist"
